- if post.favorited_by?(current_user)
  = link_to post_favorites_path(post.id), class: "position-relative mr-3 text-secondary", method: :delete, remote: true do
    %i.favorites_color.fas.fa-heart
  = post.favorites.count
- else
  = link_to post_favorites_path(post.id), class: "position-relative mr-3 text-secondary", method: :post, remote: true do
    %i.favorites_color.far.fa-heart
  = post.favorites.count
